{
  "name": "madan-resume-matcher",
  "private": true,
  "workspaces": ["apps/web", "apps/worker"],
  "scripts": {
    "dev:web": "npm run dev -w apps/web",
    "dev:worker": "npm run dev -w apps/worker",
    "build": "npm run build -w apps/web && npm run typecheck -w apps/worker"
  }
}
